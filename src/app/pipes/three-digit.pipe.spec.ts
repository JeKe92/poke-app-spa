import { ThreeDigitPipe } from './three-digit.pipe';

describe('ThreeDigitPipe', () => {
  it('create an instance', () => {
    const pipe = new ThreeDigitPipe();
    expect(pipe).toBeTruthy();
  });
});
