import { ColorTypeDirective } from './color-type.directive';

describe('ColorTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorTypeDirective();
    expect(directive).toBeTruthy();
  });
});
